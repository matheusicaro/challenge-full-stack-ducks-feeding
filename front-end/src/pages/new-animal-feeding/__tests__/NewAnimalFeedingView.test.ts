describe("Test 'NewAnimalFeedingPageView'", () => {
  test('should return expected order of child components of parent component', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('should return button with expected attributes', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('sbutton component should call submit function with parameters expected in onClick event', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });
});
