describe("Test 'Footer'", () => {
  test('should return order of child components of parent component - container', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('should return components with expected props', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('should return components with expected children', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('should return container component with expected styles', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });
});
