describe("Test 'Header'", () => {
  test('should return visualization component with expected props', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('component should receive expected props at the store', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });
});
