describe("Test 'HeaderView'", () => {
  test('should return child components in expected order', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('parent component should be returned with expected props', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('container should return total expected columns and components', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('container should return total expected columns and components', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('buttons should be aligned at the end of row ', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('should only render expected components if user is logged in', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('should only render expected components if user is not logged in', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });

  test('should return components with expected props', () => {
    /**
     * TODO: missing test due to lack of developer time
     *
     * assert(returned).toEqual(expected)
     *
     */
  });
});
