describe("Test 'AddNewAnimalFeeding'", () => {
  test('Should start component state with expected initial state values', () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });

  test('sendNewAnimalFeeding function should update component state when receiving external request data', () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });

  test('sendNewAnimalFeeding function should send new request data received to story through the increment action function', () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });

  test('sendNewAnimalFeeding function should send update state with expected values in case of request error', () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });

  test('should be called service api with expected object and parameters', () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });
});
