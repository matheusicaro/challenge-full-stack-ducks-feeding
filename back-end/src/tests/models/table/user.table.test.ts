describe("Test 'user.table.ts'", () => {
  test('Should be constructed by builder method with input generic object', async () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });

  test('Should be built empty attributes if input object is empty', async () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });

  test('Should be built instance by static builder pattern methods', async () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });
});
