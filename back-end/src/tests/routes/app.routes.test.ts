/*eslint no-undef: 0*/

describe("Test 'app.routes.test.ts'", () => {
  test('Should validade routes with expected controllers', () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });

  test('Should validade middlewares with expected responses', async () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });
});
